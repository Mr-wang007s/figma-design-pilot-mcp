# Figma OAuth App Credentials
FIGMA_CLIENT_ID=
FIGMA_CLIENT_SECRET=

# Database path (defaults to ./data.db)
DB_PATH=./data.db

# OAuth callback port (defaults to 3456)
AUTH_CALLBACK_PORT=3456

# Optional: Personal Access Token (alternative to OAuth)
FIGMA_PERSONAL_ACCESS_TOKEN=

# Bot identity prefix for replies
BOT_REPLY_PREFIX=[FCP]
